/**
 * @obinexusmk2/nexus-search
 * Core library entry point
 */

// Single authoritative re-export — core/index.ts handles all subsystems
// (algorithms, adapters, config, documents, mappers, search, storage, telemetry, types, utils)
export * from './core/index';
